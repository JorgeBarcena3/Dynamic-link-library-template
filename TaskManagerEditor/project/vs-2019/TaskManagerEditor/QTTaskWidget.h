#pragma once

#include <QWidget>
#include <QTTaskWidget.h>

class QTTaskWidget : public QWidget
{
    Q_OBJECT

public:

    QTTaskWidget(QWidget *parent = Q_NULLPTR);
    ~QTTaskWidget();

private:

};
